RDV.MEDALS.CFG = {}

RDV.MEDALS.CFG.Overhead = false -- True enables overhead, false disables overhead and switches it to a point and display on screen.

RDV.MEDALS.CFG.Reminder = (10 * 60) -- Delay for how often to remind players to check their Medals (in seconds).

RDV.MEDALS.CFG.Admins = {
    ["superadmin"] = true,
}